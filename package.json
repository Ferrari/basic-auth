{
  "name": "jwt-parser",
  "description": "jwt header parser",
  "version": "2.0.0",
  "license": "MIT",
  "keywords": [
    "authorization",
    "jwt"
  ],
  "repository": "Ferrari/jwt-parser",
  "dependencies": {
    "jsonwebtoken": "^8.0.0"
  },
  "devDependencies": {
    "istanbul": "0.4.5",
    "mocha": "3.0.2"
  },
  "files": [
    "HISTORY.md",
    "LICENSE",
    "index.js"
  ],
  "engines": {
    "node": ">= 4.8"
  },
  "scripts": {
    "test": "mocha --check-leaks --reporter spec --bail",
    "test-cov": "istanbul cover node_modules/mocha/bin/_mocha -- --reporter dot --check-leaks test/",
    "test-travis": "istanbul cover node_modules/mocha/bin/_mocha --report lcovonly -- --reporter spec --check-leaks test/"
  }
}
